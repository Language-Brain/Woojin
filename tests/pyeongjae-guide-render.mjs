import assert from 'node:assert/strict';
import handler from '../api/pyeongjae.js';
const guideId='22222222-2222-4222-8222-222222222222';
const sourceId='11111111-1111-4111-8111-111111111111';
const guide={id:guideId,entry_kind:'guide',book_no:null,sheet_no:null,volume_no:null,start_page:null,end_page:null,side:null,genre:null,title:'  ○ 『평재문집』의 구성과 수록 내용',guide_body:'<p><strong>소개</strong>입니다.</p><table><tbody><tr><td>구성</td></tr></tbody></table><a href="javascript:alert(1)" onclick="alert(1)">위험</a><a href="https://example.com" target="_blank">자료</a><script>alert(1)</script>',tags:['평재문집'],status:'published',view_count:1,created_at:'2026-09-01T00:00:00Z',updated_at:'2026-09-01T00:00:00Z',pages:[],reference_links:[]};
const nav=[{id:guideId,entry_kind:'guide',title:guide.title,created_at:guide.created_at},{id:sourceId,entry_kind:'source',title:'평재문집 제1책 제1장 앞면',book_no:1,sheet_no:1,side:'front',created_at:'2026-08-01T00:00:00Z'}];
const originalFetch=global.fetch;
global.fetch=async url=>({ok:true,status:200,json:async()=>decodeURIComponent(String(url)).includes('select=id,entry_kind')?nav:[guide]});
let body='',status=0;
const response={setHeader:()=>{},status:n=>{status=n;return response},send:v=>{body=v;return response}};
await handler({query:{id:guideId}},response);
global.fetch=originalFetch;
assert.equal(status,200);
for(const text of ['○ 『평재문집』의 구성과 수록 내용 | 삶과 언어','『평재문집』 · 안내 글','pyeongjae-guide-body','<strong>소개</strong>','<table>','https://example.com',`/pyeongjae-entry?id=${sourceId}`,'다음 자료 →','reader-text-content'])assert.ok(body.includes(text),text);
assert.ok(!body.includes('javascript:'));
assert.ok(!body.includes('onclick="alert(1)"'));
assert.ok(!body.includes('<script>alert(1)</script>'));
assert.ok(!body.includes('제null책'));
console.log('pyeongjae guide server render: PASS');
